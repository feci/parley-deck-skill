---
spec: PDS/1.0
kind: DIRECTION
handle: ledger
signature: "Every surface is a table; nothing floats, so alignment carries hierarchy."
positions: {density: dense, structure: flat}
tokens: tokens.json
states: [rest, hover, focus, pressed, disabled]
effects: [{name: corner-numeral, anchor: "the issue number of the record on screen; it changes with the record and is absent on a new one"}, {name: rule-line, anchor: "the boundary between two column groups; it disappears when a group has one column"}]
---

# Direction: ledger

Both devices carry a variable from the content, so a reader can tell what changed by
looking at them.
