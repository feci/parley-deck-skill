---
registry: core-rules/9.9.9
spec: PDS/1.0
---

# A registry whose heading carries no record

### A rule that never declared itself

The grammar puts the record immediately beneath the heading with no prose between. Here
there is prose and no record, so the rule has no machine source at all.
