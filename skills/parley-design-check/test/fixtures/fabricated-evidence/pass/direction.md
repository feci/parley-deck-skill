---
spec: PDS/1.0
kind: DIRECTION
handle: ledger
signature: "Every surface is a table; nothing floats, so alignment carries hierarchy."
positions: {density: dense, structure: flat}
tokens: tokens.json
states: [rest, hover, focus, pressed, disabled]
effects: [{name: rule-lines, anchor: "separates column groups, and disappears where a group has one column"}]
---

# Direction: ledger

The opening region carries a proof strip with one named, visibly unfilled field: the
customer count stays [unfilled] until the requester supplies it, and the strip renders the
label with the empty slot rather than a number.

Availability is quoted only where a measurement exists. source: the operations report the
brief attached.
